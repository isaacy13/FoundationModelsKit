workflows:
  build-foundation-models-tools:
    name: Build and Test FoundationModelsTools
    max_build_duration: 60
    environment:
      xcode: latest
      cocoapods: default
    triggering:
      events:
        - push
        - pull_request
      branch_patterns:
        - pattern: '*'
          include: true
    scripts:
      - name: Build Swift Package
        script: |
          swift build -c release
